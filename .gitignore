# System files
System Volume Information/
$RECYCLE.BIN/
desktop.ini
thumbs.db
.DS_Store

# Everything else will be included as per user request.
# Note: Files > 100MB will be split into chunks before upload.
# Exclude original large files before splitting
*.orig
