NeuroSentinel Frontend

Run locally:

```powershell
cd "final project/frontend"
npm install
npm run dev
```

Default dev URL: http://localhost:5173

Ensure backend is running at http://localhost:8000 for API calls.
